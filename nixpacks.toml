# Railway/Nixpacks: Node.js 20 + FFmpeg for video generation.
[variables]
NPM_CONFIG_PRODUCTION = ""
NPM_CONFIG_OMIT = "dev"

[phases.setup]
nixPkgs = ["nodejs_20", "ffmpeg"]

[phases.install]
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
